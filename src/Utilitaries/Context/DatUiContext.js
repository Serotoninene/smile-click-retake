import React, { createContext, useState } from "react";

export const DatUiContext = createContext();

export default function DatUiProvider(props) {
  // return (
  //   <DatUiContext.Provider value={{ data, handleUpdate }}>
  //     {props.children}
  //   </DatUiContext.Provider>
  // );
}
